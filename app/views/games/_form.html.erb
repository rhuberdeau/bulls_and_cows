<%= form_with(model: [@game, Guess.new], html: {class:"form-inline"}) do |form| %>

  <%= form.text_field :word, id: :guess_word, class: "form-control mb-2 mr-sm-2 mb-sm-0" %>

  <%= form.submit "Submit", class: "btn btn-primary" %>
<% end %>
